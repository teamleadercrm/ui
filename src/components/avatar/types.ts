export enum Size {
  tiny = 'tiny',
  small = 'small',
  medium = 'medium',
  large = 'large',
  hero = 'hero',
}

export enum Shape {
  circle = 'circle',
  rounded = 'rounded',
}

export enum Direction {
  horizontal = 'horizontal',
  vertical = 'vertical',
}
