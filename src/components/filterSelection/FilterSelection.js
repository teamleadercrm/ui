import React, { memo } from 'react';

import Box from '../box';

const FilterSelection = () => {
  return <Box>Hello FilterSelection!</Box>;
};

FilterSelection.propTypes = {};

FilterSelection.defaultProps = {};

export default memo(FilterSelection);
